"""
Distribución paramétrica de tokens: Temperatura, Top-k, Top-p, Softmax.

TODO:
- apply_temperature(logits, temperature)
- top_k_filter(logits, k)
- top_p_filter(logits, p)
- sample(logits, **params) -> token_id
"""
