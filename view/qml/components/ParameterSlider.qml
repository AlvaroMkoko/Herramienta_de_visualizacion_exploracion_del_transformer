// Control reutilizable para parámetros del modelo (ej. Temperatura, Top-k).
// TODO: Slider + label enlazado por data binding al ViewModel.

import QtQuick
import QtQuick.Controls

Item {
    // TODO: property alias value: slider.value
}
